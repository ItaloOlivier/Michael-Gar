# Render Blueprint for Michael Gar's static site (michaelgar.com).
# Mirrors the live Render service config so deploys are reproducible from git.

services:
  - type: web
    name: michael-gar
    runtime: static
    autoDeploy: true
    buildCommand: ""
    staticPublishPath: .
